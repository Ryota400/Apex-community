<% content_for(:title, @video.title) %>
<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
       <h3><%= t('.title') %><h3>
       <%= render 'form', { video: @video } %>
    </div>
  </div>
</div>
