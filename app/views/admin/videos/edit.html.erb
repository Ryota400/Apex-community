<% content_for(:title, @video.title) %>
<div class="container">
  <div class="row">
    <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
      <h1><%= t '.title' %></h1>
      <%= form_with model: @video, url: admin_video_path(@video), local: true do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :best_movie %>
          <br>
          <%= f.file_field :best_movie, :accept => 'video/mp4', onchange: 'previewImage(preview)'  %>
          <%= f.hidden_field :best_movie_cache %>
        </div>
        <div class='mt-3 mb-3'>
          <%= video_tag(@video.best_movie.url,
                    id: 'preview',
                    :controls => true, :autoplay => true, :muted => true,
                    size: '600x400') %>
        </div>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
