<% content_for(:title, @party_board.title) %>
<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
       <h3><%= t('.title') %><h3>
       <%= render 'form', { party_board: @party_board } %>
    </div>
  </div>
</div>
