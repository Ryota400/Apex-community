<%= link_to like_path(current_user.likes.find_by(clip_id: clip.id)),
            id: "js-like-button-for-clip-#{clip.id}",
            class:"float-right",
            method: :delete,
            remote: true do %>
  <%= icon 'fas', 'star' %>
<% end %>
